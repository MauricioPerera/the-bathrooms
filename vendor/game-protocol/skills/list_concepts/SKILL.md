---
name: list_concepts
description: List every knowledge concept this origin publishes (id, type, title, description).
version: 0.1.0
license: MIT
---

# list_concepts

List every knowledge concept this origin publishes (id, type, title, description). Generated by `llms-skills memory` from this origin's OKF
knowledge bundle; backed by a hash-verified BM25 snapshot (origin memory,
Executable Skills v0.4 §2.4). Call the tool rather than improvising: the
runtime executes it verbatim in a sandbox.
