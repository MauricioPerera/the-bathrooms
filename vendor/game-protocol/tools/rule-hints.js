/**
 * rule-hints.js — Pistas de arreglo accionables por regla, para el modo --agent del linter.
 * Cada entrada dice, en una linea, COMO corregir el hallazgo. Un agente cierra el loop
 * editar→lint→arreglar casi sin razonar. Reglas no listadas no llevan hint (no es error).
 */
module.exports = {
  // --- core / estructurales ---
  'parse-error': 'El front-matter no es YAML-subset valido. Las listas van en flujo [a, b] (no "- item"); cada linea lleva ":"; comas/":" dentro de texto van entre comillas.',
  'frontmatter-present': 'Envuelve los tokens entre `---` al inicio y `---` antes del cuerpo Markdown.',
  'required-fields': 'Anade el campo que falta al front-matter (p.ej. `version: "0.1"`, `name: ...`, `profile: <id>`).',
  'profile-known': 'Usa un `profile` que exista en profiles/. Consulta manifest.json para la lista.',
  'profile-load-error': 'El archivo del perfil existe pero tiene un error de sintaxis/carga. Revisa profiles/<id>.js.',
  'version-migration': 'El `version` del GAME.md no coincide con la specVersion soportada por el tooling. Si es anterior, actualiza el archivo segun MIGRATION.md (la version vieja se remueve en la major siguiente). Si es posterior, actualiza el tooling.',
  'section-order': 'Reordena los encabezados `##` segun `sections` del perfil (ver manifest.json) o renombra el no-canonico.',
  'text-valid': 'El valor de cada clave de `text` debe ser una cadena no vacia.',
  // --- familia broken-ref (referencias) ---
  'broken-ref': 'El valor referenciado no existe en la coleccion destino. Crea esa entrada o corrige el nombre (ver `references` del perfil en manifest.json).',
  'move-type-valid': 'El `type` del ataque debe existir en `types` (o ser NORMAL).',
  'species-type-valid': 'El `type` de la especie debe existir en `types` (o ser NORMAL).',
  'moves-exist': 'Cada ataque en `species.*.moves` debe estar declarado en `moves`.',
  'trainer-team-valid': 'Cada especie del equipo del entrenador debe existir en `species`.',
  'encounter-ref': 'Cada especie de `encounters` debe existir en `species`.',
  'sprite-ref': 'El `sprite` referenciado debe existir en `sprites`.',
  'player-ref': 'Revisa player.starter/inventory/level/start: deben referenciar entradas existentes y tener tipos validos.',
  'warp-ref': 'El `to` del warp debe ser una escena existente.',
  'warp-lock': 'El item de `warp.locked` debe ser otorgado por algun pickup, o el juego es imposible.',
  'goal-lock': 'El item de `goal.locked` debe ser otorgado por algun pickup.',
  // --- rangos / dimensiones ---
  'palette-range': 'El indice de paleta debe estar en 0..palettesCount-1.',
  'palette-color-range': 'Cada color es [r,g,b] con valores en 0..31.',
  'sprite-dims': 'Los sprites deben ser 16x16: matriz de numeros o forma compacta (16 strings hex de 16 chars, 1 char = 1 celda 0..15).',
  'tileart-dims': 'tileArt debe ser 8x8 con indices de color en 0..palettesCount-1: matriz de numeros o forma compacta (8 strings hex de 8 chars).',
  'tileart-ref': 'El id de tileArt debe estar registrado en `tiles` y en el rango valido.',
  'sfx-valid': 'Cada sfx necesita freq (>0 Hz) y dur (0-5 s).',
  'economy-bounds': 'Los precios deben ser > 0; catchBase+catchScale en [0,1].',
  'trainer-bounds': 'El premio del entrenador debe ser > 0 y su `team` una lista no vacia de especies.',
  'move-bounds': 'power del ataque > 0 (si se declara); chance en [0,1].',
  'species-bounds': 'maxhp > 0; si declaras evolvesInto, declara tambien atLevel (> 0) o la evolucion se exporta sin nivel.',
  'encounter-zone': 'Cada area de `encounters` debe existir como area de `overworld` o como mapa en `maps` (o el motor no la disparara).',
  'tile-id-range': 'Los ids del registro `tiles` van en 16..63 (mismo rango de plataforma que tileArt).',
  'sprite-4bpp': 'Cada celda del sprite es un nibble 4bpp: entero en 0..15 (indexa spritePalettes).',
  'palette-size': 'Maximo 16 colores por paleta: el export trunca a 16 y los extra se pierden en silencio.',
  'item-effect-valid': 'effect en {heal,cure,catch}; heal requiere amount>0; cure requiere `cures`.',
  // --- mapas / escenas / entidades ---
  'map-dims': 'Las filas del mapa deben coincidir con platform.rows x platform.cols.',
  'map-legend-ref': 'Cada simbolo de la leyenda (y fill) debe referenciar un tile y paleta validos.',
  'map-meta': 'entry/exit/return dentro de limites; exit sobre un felpudo (tile 46).',
  'scene-dims': 'Todas las filas de la escena deben tener el mismo ancho.',
  'scene-rows': 'La escena/mapa necesita al menos una fila en `rows` (no puede estar vacio).',
  'scene-legend-ref': 'Cada simbolo de la escena debe referenciar un tile existente.',
  'entity-bounds': 'La entidad (npc/pickup/warp/goal/enemy) debe estar dentro de la escena.',
  'entity-tile': 'El `tile` de la entidad debe existir en `tiles`.',
  'entity-text': 'El `dialogue` del NPC debe ser una clave existente en `text`.',
  'entity-item': 'El pickup necesita un `item` (el nombre de lo que otorga).',
  'goal-missing': 'Ninguna escena tiene `goal`: agrega uno o el juego no se puede ganar (warn, no bloquea el lint).',
  'win-text': 'Agrega `win.text` con el mensaje de victoria (warn, no bloquea el lint).',
  'player-start': 'player.start debe caer dentro de la escena/escena inicial.',
  // --- overworld / cross-checks con el motor (monster-rpg) ---
  'overworld-ref': 'En cada area de `overworld`: NPC/trainer/warp con col en 0..platform.cols-1 y row en 0..platform.rows-1; NPC con `dialogue` (si el texto lleva comas, entre comillas); `warp.target` debe ser un area de overworld o un mapa declarado; `trainer.name` debe existir en `trainers`.',
  'type-symmetry': 'La tabla `types` debe ser simetrica: si A->B es x2, entonces B->A debe ser x0.5 (y viceversa x0.5 <-> x2). Corrige el multiplicador inverso.',
  'solid-sync': 'El flag `solid` de cada tile en GAME.md debe coincidir con el Set SOLID_TILES del motor. Pon el tile en SOLID_TILES si es solid, o quitalo / cambia `solid: false` si es caminable. (Cross-check opcional via GAME_ENGINE.)',
  'dead-token': 'La clave de `balance` no aparece en el motor (gBal(...), .clave o ["clave"]). Referenciala en el codigo del motor o elimina la declaracion de GAME.md. (Cross-check opcional via GAME_ENGINE.)',
  // --- generador (roguelike) ---
  'generator-missing': 'Anade el bloque `generator`.',
  'generator-field': 'Completa los campos requeridos del generador (seed, roomW, roomH, maxDepth).',
  'generator-size': 'roomW y roomH deben ser >= 5.',
  'generator-tile': 'Los tiles que usa el generador (floor/wall/door/enemy/goal/stairs) deben existir en `tiles`.',
  'enemypool-tile': 'El tile de cada enemigo del pool debe existir en `tiles`.',
  'itempool-tile': 'El tile de cada item del pool debe existir en `tiles`.',
  'itempool-kind': 'kind de item debe ser `heal` o `weapon`.',
  'itempool-amount': 'Un item heal necesita amount > 0.',
  'itempool-power': 'Un item weapon necesita power > 0.',
  'player-tile': 'player.tile debe existir en `tiles`.',
  'player-hp': 'player.hp debe ser > 0.',
  'enemy-hp': 'El hp del enemigo debe ser > 0.',
  // --- advance-wars (sprites GBA extraidos) ---
  'unit-palette-ref': 'El `palette` de la unidad debe ser una clave existente de `palettes`.',
  'unit-dims': 'La unidad necesita width/height enteros > 0 y `tileData` con exactamente height filas de width celdas.',
  'unit-tiledata-range': 'Cada celda de tileData es un nibble 4bpp: entero en 0..15 (indexa la paleta de 16 colores).',
  // --- papers-please ---
  'day-entrant-ref': 'Cada id de `days.*.entrants` debe existir en `entrants`.',
  'day-rule-ref': 'Cada id de `days.*.rules` debe existir en `rules`.',
  'rule-doc-ref': 'El `document` de la regla (si se declara) debe existir en `documents`.',
  'rule-country-ref': 'El `country` de la regla (si se declara) debe existir en `countries`.',
  'rule-docs-ref': 'Cada id de `rules.*.documents` debe existir en `documents`.',
  'rule-type-valid': 'El `type` de la regla debe ser uno soportado por el motor (require-document | ban-country | require-field-match | not-expired).',
  'entrant-decision': 'La decision del solicitante debe ser approve/deny/detain.',
  'entrant-doc-ref': 'Cada documento que porta un solicitante (`entrants.*.docs`) debe existir en `documents`.',
  'entrant-doc-field': 'El campo declarado en un documento portado debe estar en `documents.<tipo>.fields` (warn, no bloquea el lint).',
  // --- crafting ---
  'recipe-output': 'El `output` de la receta debe existir en `items`.',
  'recipe-station': 'La `station` de la receta (si se declara) debe existir en `stations`.',
  'recipe-inputs': 'Los inputs de la receta deben referenciar materiales existentes con cantidad > 0.',
  'material-bounds': 'El `stack` del material (si se declara) debe ser > 0.',
  // --- platformer ---
  'enemy-ref': 'Cada enemigo de `levels.*.enemies` debe existir en `enemies`.',
  'tileset-ref': 'El `tileset` del nivel (si se declara) debe existir en `tilesets`.',
  'spawn-ref': 'player.spawnLevel (si se declara) debe existir en `levels`.',
  'physics-bounds': '`physics.gravity`, `physics.jump` y `physics.runSpeed` deben ser > 0.',
  'level-goal': 'level.goal (si se declara) necesita x/y numericos.',
  // --- voxel ---
  'prefab-fill-ref': 'El `fill` del prefab (si se declara) debe existir en `materials`.',
  'prefab-cell-ref': 'El `m` (material) de cada celda del prefab debe existir en `materials`.',
  'structure-prefab-ref': 'Cada `place.*.prefab` de la estructura debe existir en `prefabs`.',
  'material-color': 'El color del material es [r,g,b] con valores 0..255.',
  'prefab-size': 'El size del prefab son 3 enteros > 0.',
  'prefab-empty': 'El prefab no tiene `fill` ni `cells`: no dibuja nada (warn, no bloquea el lint).',
  'prefab-cell': 'Cada celda del prefab tiene coord entera dentro del size.',
  'structure-at': 'El `at` de un placement son 3 enteros.',
  // --- quiz (perfil puro-datos de referencia) ---
  'question-category-ref': 'El `category` de la pregunta debe existir en `categories`.',
  'round-question-ref': 'Cada id de `rounds.*.questions` debe existir en `questions`.',
  'question-points': 'Cada pregunta necesita `points` > 0.',
  'question-time': '`seconds` es entero en 5..120 (si se declara).',
  'question-difficulty': '`difficulty` obligatorio: easy | normal | hard.',
  'round-reward': '`reward` de la ronda debe ser >= 0 (si se declara).',
  // --- roguelike: extensiones (cerraduras/llaves, jefe, progresion) ---
  'generator-chance': 'Los porcentajes del generator (branch/floorChance/itemChance/lockChance/keyChance) van en 0..100.',
  'boss-fields': '`boss` necesita tile existente y hp/damage enteros > 0.',
  'progression-fields': '`progression`: killsPerAtk entero > 0, maxBonus entero >= 0, permadeath boolean.',
  // --- peg-solitaire (perfil puro-datos) ---
  'board-goal': '`goal` obligatorio: clear (dejar 1 peg) | center (dejar 1 peg en el centro).',
  'board-difficulty': '`difficulty` obligatorio: easy | normal | hard.',
  // --- sudoku (perfil puro-datos) ---
  'player-start-ref': '`player.start` debe existir en la coleccion inicial del perfil (p.ej. `puzzles` o `boards`).',
  'puzzle-difficulty': '`difficulty` obligatorio: easy | normal | hard.',
  'sudoku-balance': '`lives` y `hints` enteros >= 0 (si se declaran).',
  // --- shooter (perfil puro-datos) ---
  'ship-weapon-ref': 'El `weapon` de la nave debe existir en `weapons`.',
  'enemy-weapon-ref': 'El `weapon` del enemigo (opcional) debe existir en `weapons`.',
  'wave-enemy-ref': 'Cada `enemy` de `waves.*.spawns` debe existir en `enemies`.',
  'player-ship-ref': '`player.ship` debe existir en `ships`.',
  'ship-bounds': '`speed` y `hp` de la nave son requeridos y > 0 (hp entero).',
  'weapon-bounds': '`damage`, `rate` y `bulletSpeed` del arma son requeridos y > 0.',
  'enemy-behavior': '`behavior` obligatorio: chaser | drifter (un valor nuevo exige soporte del motor).',
  'powerup-effect': '`effect` obligatorio: heal | rapid | shield.',
  'powerup-amount': '`amount` del powerup > 0 (si se declara).',
  'powerup-duration': '`duration` del powerup entero > 0 (si se declara).',
  'arena-bounds': '`arena.width` y `arena.height` requeridos y > 0.',
  'balance-bounds': '`powerupChance` en [0,1]; `lives` entero >= 0.',
  // --- tower-defense ---
  'tower-dmgtype-valid': 'El `dmgType` de la torre debe existir en `dmgTypes`.',
  'wave-enemy-valid': 'Cada `enemy` de `waves.*.spawns` debe estar declarado en `enemies`.',
  'tower-sprite-ref': 'El `sprite` de la torre debe existir en `sprites`.',
  'enemy-sprite-ref': 'El `sprite` del enemigo debe existir en `sprites`.',
  'tower-cost-valid': 'El `cost` de la torre debe ser > 0.',
  'tower-range': 'El `range` de la torre debe ser > 0.',
  'tower-damage': 'El `damage` de la torre debe ser > 0.',
  'tower-rate': 'El `rate` (cadencia) de la torre debe ser > 0.',
  'enemy-bounds': 'Cada enemigo necesita hp>0, speed>0 y bounty>0.',
  'enemy-armor-valid': 'El `armor` del enemigo debe ser un id de la lista `armors`.',
  'wave-valid': 'Cada oleada necesita `spawns` (lista no vacía); cada spawn necesita count>0 y gap>=0.',
  'wave-monotonic': 'El HP total por oleada no deberia decaer (balance smell): sube el count o el hp de oleadas tardias.',
  'economy-balance': 'economy.startGold/startLives > 0; balance.sellRatio en [0,1]; balance.interestRate >= 0.',
  'dmgtype-armor-valid': 'Cada clave de cada `dmgTypes.*` debe ser un id de `armors`.',
  'dmgtype-mult': 'Cada multiplicador de `dmgTypes` debe ser un numero > 0.',
  'dmgtype-symmetry': 'Un dmgType con todos sus multiplicadores iguales no aporta counters: dale varianza (fuerte vs algun armor, debil vs otro).',
  'path-contiguous': 'Cada par consecutivo de `maps.*.path` debe ser ortogonalmente adyacente (|dcol|+|drow| == 1).',
  'path-bounds': 'Cada waypoint de `maps.*.path` debe caer dentro del mapa (0..platform.cols-1, 0..platform.rows-1).',
};
